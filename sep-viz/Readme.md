# Sep-Viz

This directory contains a TypeScript project that takes Alectryon-generated HTML files (without diagrams) and produces visualized versions in which separation-logic formulas are rendered as diagrams.

## How to build

Require `node.js`.

If using Nix, prefix the `npm` commands below with `USE_NIX=1`.

Install dependencies: `npm install`

Build: `npm run build`
