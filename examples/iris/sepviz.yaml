constr:
  # iris
  '$Basic':
    label: 'Basic'
    drawBorder: 1
  '$Pair':
    label: 'Pair'
    drawBorder: 1
  # queue.v
  'isQueue':
    args:
      0:
        inTable: true
  'isListSeg':
    inPort: 'portL'
    args:
      0:
        inTable: true
        inPort: 'portL'
      1:
        forceEdge: true
        outPort: 'portL'
  # linked_list.v
  'isList':
    args:
      0:
        inTable: true
value:
  # iris
  '$LitV':
    label: '#$1'
    uid: '$1'
