{
  "name": "visualizer",
  "private": true,
  "workspaces": ["sep-viz", "alectryon-viz"],
  "scripts": {
    "build:all": "npm run build -w sep-viz && npm run build -w alectryon-viz",
    "clean": "rm -rf node_modules sep-viz/node_modules alectryon-viz/node_modules",
    "reinstall": "npm run clean && npm install"
  }
}
